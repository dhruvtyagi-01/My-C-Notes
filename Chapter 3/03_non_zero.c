#include <stdio.h>

int main() {
     if (1) {
        printf("This if is executed\n");
     }
     if (533) {
        printf("This if is also executed\n");
     }
     if (8.76) {
        printf("This if is also executed\n");
     }
     if ('c') {
        printf("This if is also executed\n");
     }
     if (0) {
        printf("I am zero I am no executed");
     }
     
    return 0;
}